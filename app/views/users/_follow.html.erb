<%= form_with(model: current_user.active_relationships.build) do |f| %>
    <div>
        <%= hidden_field_tag :following_id, @user.id %>
    </div>
    <%= f.submit t('user.follow'), class: "btn-follow btn-primary" %>
<% end %>
  