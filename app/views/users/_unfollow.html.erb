<%= form_with(model: current_user.active_relationships.find_by(following_id: @user.id),
             html: { method: :delete }, local: true) do |f| %>
  <%= f.submit t('user.unfollow'), class: "btn-unfollow" %>
<% end %>
